# v0.1.3（2026-09-04）

## 🐞 修复

- 详情页「下载本书」422：JM 上游返回的书籍 id 是 int，parse 层原样透传导致回传
  `bookId: str` 校验失败（收藏接口同款字段一并修复）；`_parse_book_info` /
  `_parse_book_detail` / `_parse_categories` 统一 `str()` 规范 id 与 epsId（`33ca08d`）
- 评论内容显示 `<div style='...'>` 标签：上游评论是富文本，parse 层新增
  `_clean_comment_text` 剥标签、`<br>` 转换行、还原 HTML 转义；前端评论加
  `whitespace-pre-line` 让换行可见（`ed96cf8`）

## 💄 界面

- Windows/Linux 隐藏窗口顶部默认菜单栏（File/Edit/View/...）；macOS 保留系统
  菜单栏以免失去 Cmd+C/V 快捷键（`17fedf0`）
