# v0.1.1（2026-09-04）

## 🐞 修复

- 打包后白屏：vite `base` 设为 `./` 相对路径，适配 file:// 加载（`0653db5`）

## 🔧 构建与 CI

- main push 不再自动构建，仅打 tag（`v*`）触发构建并发草稿 Release（`3834220`）

## 已知问题

- 产物文件名仍为 `JMComic-0.1.0-win.zip`：electron-builder 读 package.json 版本而非 tag。CI 已补版本同步步骤（`7cdbb73`），下个 tag 起生效
